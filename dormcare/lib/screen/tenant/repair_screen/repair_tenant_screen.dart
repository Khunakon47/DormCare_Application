import 'package:flutter/material.dart';

class RepairTenantScreen extends StatelessWidget {
  const RepairTenantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}