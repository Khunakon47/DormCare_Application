import 'package:flutter/material.dart';

class AlterTenantScreen extends StatelessWidget {
  const AlterTenantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}